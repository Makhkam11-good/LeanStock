services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leanstock-api
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-3000}:3000"
    environment:
      SERVICE_ROLE: api
      NODE_ENV: ${NODE_ENV:-${ENVIRONMENT:-development}}
      APP_PORT: ${APP_PORT:-}
      BACKEND_PORT: ${BACKEND_PORT:-3000}
      APP_HOST: 0.0.0.0
      DATABASE_URL: ${COMPOSE_DATABASE_URL:-postgresql://leanstock:leanstock_pass@postgres:5432/leanstock}
      COMPOSE_DATABASE_URL: ${COMPOSE_DATABASE_URL:-}
      PLATFORM_DATABASE_URL: ${DATABASE_URL:-}
      DOKKU_POSTGRES_AQUA_URL: ${DOKKU_POSTGRES_AQUA_URL:-}
      JWT_SECRET: ${JWT_SECRET:-}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-dev-jwt-secret-min-32-chars-change-in-production-please}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-}
      JWT_REFRESH_SECRET_KEY: ${JWT_REFRESH_SECRET_KEY:-dev-refresh-secret-min-32-chars-change-in-production-please}
      JWT_ACCESS_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 7d
      CORS_ORIGIN: ${CORS_ORIGIN:-}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3001}
      APP_BASE_URL: ${APP_BASE_URL:-}
      API_URL: ${API_URL:-}
      PUBLIC_API_URL: ${PUBLIC_API_URL:-}
      FRONTEND_URL: ${FRONTEND_URL:-}
      PUBLIC_FRONTEND_URL: ${PUBLIC_FRONTEND_URL:-}
      MAKHKAM11_GOOD_LEANSTOCK_FRONTEND_URL: ${MAKHKAM11_GOOD_LEANSTOCK_FRONTEND_URL:-}
      RATE_LIMIT_AUTH_MAX: 100
      RATE_LIMIT_AUTH_WINDOW_MS: 60000
      BCRYPT_ROUNDS: 10
      DEAD_STOCK_THRESHOLD_DAYS: 30
      DEAD_STOCK_DECAY_PERCENT: 10
      DEAD_STOCK_DECAY_MAX_PERCENT: 50
      CRON_ENABLED: true
      DEAD_STOCK_DECAY_SCHEDULE: "0 2 * * *"
      DEFAULT_PAGE_SIZE: 20
      MAX_PAGE_SIZE: 100
      APP_LOG_LEVEL: info
      SYSTEM_USER_ID: system
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      REDIS_REQUIRED: true
      EMAIL_PROVIDER: ${EMAIL_PROVIDER:-}
      EMAIL_FROM: ${EMAIL_FROM:-}
      EMAIL_FROM_ADDRESS: ${EMAIL_FROM_ADDRESS:-}
      SENDGRID_API_KEY: ${SENDGRID_API_KEY:-}
      EMAIL_API_KEY: ${EMAIL_API_KEY:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - leanstock-network

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leanstock-worker
    restart: unless-stopped
    command: node src/startService.js
    environment:
      SERVICE_ROLE: worker
      NODE_ENV: ${NODE_ENV:-${ENVIRONMENT:-development}}
      APP_PORT: ${APP_PORT:-}
      BACKEND_PORT: ${BACKEND_PORT:-3000}
      APP_HOST: 0.0.0.0
      APP_BASE_URL: ${APP_BASE_URL:-}
      API_URL: ${API_URL:-}
      PUBLIC_API_URL: ${PUBLIC_API_URL:-}
      CORS_ORIGIN: ${CORS_ORIGIN:-}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3001}
      DATABASE_URL: ${COMPOSE_DATABASE_URL:-postgresql://leanstock:leanstock_pass@postgres:5432/leanstock}
      COMPOSE_DATABASE_URL: ${COMPOSE_DATABASE_URL:-}
      PLATFORM_DATABASE_URL: ${DATABASE_URL:-}
      DOKKU_POSTGRES_AQUA_URL: ${DOKKU_POSTGRES_AQUA_URL:-}
      JWT_SECRET: ${JWT_SECRET:-}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-dev-jwt-secret-min-32-chars-change-in-production-please}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-}
      JWT_REFRESH_SECRET_KEY: ${JWT_REFRESH_SECRET_KEY:-dev-refresh-secret-min-32-chars-change-in-production-please}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      REDIS_REQUIRED: true
      EMAIL_PROVIDER: ${EMAIL_PROVIDER:-}
      EMAIL_FROM: ${EMAIL_FROM:-}
      EMAIL_FROM_ADDRESS: ${EMAIL_FROM_ADDRESS:-}
      SENDGRID_API_KEY: ${SENDGRID_API_KEY:-}
      EMAIL_API_KEY: ${EMAIL_API_KEY:-}
      SYSTEM_USER_ID: system
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "const http=require('http');const ports=[process.env.PORT,process.env.APP_PORT,5000,3000].filter(Boolean);let i=0;function check(){const port=Number(ports[i++]);if(!port)return process.exit(1);const req=http.get({host:'127.0.0.1',port,path:'/health',timeout:1500},res=>process.exit(res.statusCode<500?0:1));req.on('error',check);req.on('timeout',()=>{req.destroy();check();});}check();",
        ]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3
    networks:
      - leanstock-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api/v1}
    container_name: leanstock-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3001}:80"
    environment:
      API_PROXY_URL: ${API_PROXY_URL:-${MAKHKAM11_GOOD_LEANSTOCK_API_URL:-http://api:3000}}
    depends_on:
      - api
    networks:
      - leanstock-network

  postgres:
    image: postgres:15-alpine
    container_name: leanstock-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: leanstock
      POSTGRES_PASSWORD: leanstock_pass
      POSTGRES_DB: leanstock
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init-test-db.sql:/docker-entrypoint-initdb.d/10-init-test-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U leanstock -d leanstock"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - leanstock-network

  redis:
    image: redis:7-alpine
    container_name: leanstock-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - leanstock-network

volumes:
  postgres-data:

networks:
  leanstock-network:
    driver: bridge
